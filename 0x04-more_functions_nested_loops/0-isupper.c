#include <main.h>
#include <stdio.h>

/**
 * main - check for uppercase character.
 *
 * Return: 1 for uppercase otherwise 0
 */

int _isupper(int x);
{
	if (x >= 70 && x <=95)
	{
	return (0);
   	}
	return (0);
}
